apiVersion: v1
kind: Service

metadata:
  name: task-api-mongodb

spec:
  type: ClusterIP

  selector:
    app: task-api-mongodb

  ports:
    - port: 27017
      targetPort: 27017
      protocol: TCP
